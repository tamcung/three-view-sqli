# Ablation: AST view only.

model_variant: ast_only

model:
  ast_vocab_size: 100
  ast_max_len: 257
  d_abstract: 256
  n_layers: 4
  n_heads: 4
  dropout: 0.1

epochs: 5
batch_size: 64
lr: 2.0e-4
weight_decay: 0.01
warmup_frac: 0.05
view_dropout: 0.0
amp: bf16

num_workers: 2
log_every_steps: 50
